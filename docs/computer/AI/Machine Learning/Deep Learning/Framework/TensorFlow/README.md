[TensorFlow](https://www.tensorflow.org/) 是由 Google 開發，用來部署與實踐深度學習 (Deep Learning) 的架構。

> [!NOTE]
> 張量 (Tensor) 在深度學習中，用來展示資料的方法。最常見的方式就是使用多個二維矩陣，用來展示更多維度。

TensorFlow 的核心是使用 C++ 開發，並提供了 Python 與 C++ 的介面。